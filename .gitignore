# dependencies
node_modules
.pnpm-store

# next.js / build
.next
out
build
dist
*.tsbuildinfo
next-env.d.ts

# env — never commit secrets (see .env.example)
.env
.env.*
!.env.example

# test / coverage
coverage
playwright-report
test-results
blob-report

# deploy
.vercel

# xrpl feedback hook (per-user config lives in ~/.xrpl-feedback-hook.json, not here)

# misc
*.log
.DS_Store
.turbo
.idea
.vscode/*
!.vscode/extensions.json
.pglite
