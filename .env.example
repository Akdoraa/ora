# Root / shared env example. Copy to .env and fill as needed.
# DATABASE_URL is used by packages/db (Prisma). API and worker may use it via @ora/db.

DATABASE_URL=postgresql://ora:ora@localhost:5432/ora
REDIS_URL=redis://localhost:6379

# API (apps/api)
PORT=3001
